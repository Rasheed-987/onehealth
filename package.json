{
  "name": "onehealth",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tailwindcss -i ./src/input.css -o ./assets/css/app.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ./assets/css/app.css --minify",
    "vercel-build": "npm run build && rm -rf public && mkdir -p public && cp -r assets *.html public/",
    "frames": "bash tools/vidframes.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
